import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/header";
import { Footer } from "@/components/footer";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Odds.cn - Compare Sports Betting Odds | Best Lines & Sportsbook Reviews",
  description: "Compare odds from top sportsbooks. Find the best betting lines for NFL, NBA, MLB, NHL, Soccer and more. Expert reviews and betting guides.",
  keywords: "sports betting, odds comparison, betting odds, sportsbook reviews, NFL odds, NBA odds, betting lines",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased bg-zinc-950 text-zinc-100 min-h-screen flex flex-col`}>
        <Header />
        <main className="flex-1">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
